using PaymentInternship.Models;
using System.Net;

/*namespace PaymentInternship.Controllers;

public static class LoginEndpointsClass
{
    public static Response response = new Response();


    public static void MapLoginEndpoints (this IEndpointRouteBuilder routes)

    {
        
        Login login = new Login();



        routes.MapPost("/api/Login", (Login model) =>
        {
            //return Results.Created($"/Logins/{model.ID}", model);
            
            login = model;
            if (true)
            {
                response.ResponseMessage = "Succees";
            }
            else
                response.ResponseMessage = "Failure";


        })
        .WithName("CreateLogin");


    }
}*/
